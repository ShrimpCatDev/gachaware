local gw,gh=conf.gW,conf.gH

return{
    lg=lg,
    math=math,
    print=print,
    tostring=tostring,
    pal=pal,
    input=input,
    table=table,
    string=string,
    conf={
        gW=gw,
        gH=gh
    },
    timer=timer,
    lerp=lerpDt,
    clamp=clamp,
    pairs=pairs,
    ipairs=ipairs,
    anim8=anim8,
    col=collision
}